{
  "name": "myroom",
  "private": true,
  "scripts": {
    "version:patch": "npm version patch --no-git-tag-version --prefix frontend",
    "version:minor": "npm version minor --no-git-tag-version --prefix frontend",
    "version:major": "npm version major --no-git-tag-version --prefix frontend"
  }
}
